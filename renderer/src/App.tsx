import { useState } from 'react'
import styled from 'styled-components'

const App = () => {
  return (
    <AppWrapper>
      <header>1234</header>
    </AppWrapper>
  )
}

const AppWrapper = styled.article`
  width: 100vw;
  height: 100vh;
  background: #000;
`

export default App
